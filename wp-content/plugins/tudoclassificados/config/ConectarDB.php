<?php
class ConectarDB
{      
      // private function conectarDB(string $table)
      // {
      //       $mysql = new mysqli('localhost', DB_USER, DB_PASSWORD, $table);
      //       $mysql->set_charset('utf8');//utf8mb4

      //       if ($mysql == FALSE) {
      //             echo "Erro na conexão";
      //             exit();
      //       }

      //       if($table == $this->tablePost){
      //             $this->mysqlPost = $mysql;
      //             return;
      //       }
            
      // }

      // // protected function tablePost()
      // // {
      // //       //$tablePostMeta = 'class_postmeta';
      // //       $tablePost = 'class_posts';
      // //       $this->conectarDB($tablePost);
      // //       return 'CONECTAR TABLE POST';
      // // }
}
